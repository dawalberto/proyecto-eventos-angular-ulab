import { EventOrderByPipe } from './event-order-by.pipe';

describe('EventOrderByPipe', () => {
  it('create an instance', () => {
    const pipe = new EventOrderByPipe();
    expect(pipe).toBeTruthy();
  });
});
